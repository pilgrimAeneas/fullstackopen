import ReactDOM from 'react-dom/client'

import App from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(<App />)

// Always keep the console open, as software development is already hard as is,
// and doesn't need to be harder by not using the available tools, of which,
// the console and debug printing might be the best.